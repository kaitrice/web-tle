import { TourWidget } from "./BiTWidget";

export default function Tour() {
    return (
        <section id="tour" >
            <h1 className="uppercase tracking-widest text-2xl md:text-4xl font-bold">Live Shows</h1>
            <TourWidget />
        </section>
    )
}